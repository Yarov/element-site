services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: elementspa
      POSTGRES_USER: elementspa
      POSTGRES_PASSWORD: elementspa
    ports:
      - "5432:5432"
    volumes:
      - elementspa-postgres:/var/lib/postgresql/data

volumes:
  elementspa-postgres:
